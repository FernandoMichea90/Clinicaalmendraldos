import React,{useState} from 'react'

const useAutenticado = (props) => {
  const [usuariaAutenticado, setusuariaAutenticado] = useState(null)
  const administrador=async(user)=>{
var result=false

  } 
  return (
        <div>
            
        </div>
    )
}

export default useAutenticado
